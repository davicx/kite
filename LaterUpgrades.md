# Later upgrades (Kite)

## FRONT END (Kite)

### Chat (`ChatPage` / `chatAPI`)

- [ ] **Chat send UX** — Today: submit → `POST /message` completes → refetch conversation messages → bubbles appear. Fine for now. Later: optimistic local bubble for the user line so it feels instant; keep assistant/server rows appearing after the API returns (optionally tighten refetch-on-success vs relying on the poll interval for perceived latency).

## To do (other)

_(Add Kite-wide items here as needed.)_
